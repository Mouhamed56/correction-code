# correction-Voici un composant HTML/CSS de carte de prix qui contient des erreurs et n’est pas réutilisable. Je veux que tu le répares complètement et que tu le transformes en un composant propre, moderne et personnalisable.

Objectifs :
1. Identifier et corriger tous les bugs dans le code (ex : balise <h2> non fermée, styles incomplets, mise en page cassée).
2. Améliorer la mise en page : responsive, spacing propre, box-shadow, border-radius, bon alignement.
3. Rendre le bouton fonctionnel : ajouter un script minimal qui émet un événement (ex: console.log ou CustomEvent).
4. Transformer le code en un composant réutilisable :
   - soit sous forme de fonction JS : createPricingCard({title, price, features, cta})
   - soit Web Component <pricing-card>
   → Choisir l’option la plus propre et bien structurée.
5. Le composant doit accepter :
   - titre personnalisable
   - prix personnalisable
   - liste de fonctionnalités dynamique
   - texte du bouton personnalisable
6. Nettoyer le HTML, le CSS, moderniser la structure, et assurer une bonne accessibilité.
7. Retourner un fichier complet (HTML + CSS + JS) immédiatement testable dans le navigateur.code
correction code
